# Focus List

A compact task tracker that runs as a static web app. It lets you add tasks, mark them complete, filter the list, clear finished work, and keeps data in local storage.

## Run locally

Open `index.html` in a browser, or serve the folder with any static file server.
